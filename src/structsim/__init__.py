"""structsim — a structured-simulation framework.

Python migration of the SiLab group's Java ``structSim`` framework. The
framework runs structured simulations: it reads a parameter file and a
configuration file, applies a tree of parameter *modifiers* to explore a
space of simulation environments, runs a simulator for each, and collects the
results.

See the top-level ``README.md`` for provenance and usage.
"""

__version__ = "0.1.0"
